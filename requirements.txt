# Core dependencies for universal_rally_detector.py
torch>=2.0.0
transformers>=4.40.0
opencv-python>=4.8.0
Pillow>=9.0.0
numpy>=1.24.0
tqdm>=4.65.0

# Additional dependencies that may be needed
accelerate>=0.20.0
tokenizers>=0.15.0
requests>=2.28.0  # For vLLM OpenAI API client

# Performance optimization with vLLM (optional, for faster inference)
vllm>=0.11.0  # For Qwen3-VL support, use >=0.6.1 for Qwen2.5-VL only
ray>=2.0.0    # Required by vLLM for distributed computing
tqdm==4.66.5  # Compatible version with vLLM 0.11.0

# Optional dependencies for better performance
ffmpeg-python>=0.2.0
av>=10.0.0
dashscope>=1.25.1